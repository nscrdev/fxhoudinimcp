[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "fxhoudinimcp"
dynamic = ["version"]
description = "Comprehensive MCP server for SideFX Houdini with 195 tools covering SOPs, LOPs/USD, DOPs, PDG/TOPs, COPs, HDAs, animation, rendering, VEX, and more"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["houdini", "mcp", "model-context-protocol", "vfx", "3d", "usd", "procedural", "sidefx"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Multimedia :: Graphics :: 3D Modeling",
]
dependencies = [
    # Floor is 1.14.0, not 1.8.0. Every module here uses
    # `from __future__ import annotations`, so parameter annotations are strings,
    # and FastMCP could not resolve them until 1.14: earlier versions die with
    # "TypeError: issubclass() arg 1 must be a class" while registering a tool.
    # The old >=1.8.0 was therefore a floor the package never actually supported.
    #
    # 2.x works too. mcp 2.0 renamed the server class out of mcp.server.fastmcp,
    # made Tool.inputSchema into input_schema and stopped injecting Context into
    # static resources; fxhoudinimcp._sdk is the single import site that knows.
    # Capped below 3 so the next such change fails at install time with a
    # resolver message rather than at import time with a traceback.
    "mcp>=1.14.0,<3",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=5.0",
    "ruff>=0.8.0",
]
docs-markdown = [
    "html2text>=2024.2.26",
]
mkdocs = [
    "mkdocs-material",
    "mkdocs-gen-files",
    "mkdocs-literate-nav",
    "mkdocs-section-index",
    "mkdocs-git-revision-date-localized-plugin",
    "mkdocs-git-authors-plugin",
    "mkdocs-git-committers-plugin-2",
    "mkdocstrings[python]",
    "mike",
    "black",
]

[project.scripts]
fxhoudinimcp = "fxhoudinimcp.__main__:main"

[project.urls]
Homepage = "https://github.com/healkeiser/fxhoudinimcp"
Documentation = "https://healkeiser.github.io/fxhoudinimcp"
Repository = "https://github.com/healkeiser/fxhoudinimcp"
Issues = "https://github.com/healkeiser/fxhoudinimcp/issues"

[tool.hatch.version]
source = "vcs"

[tool.hatch.version.raw-options]
version_scheme = "no-guess-dev"

[tool.hatch.build.hooks.vcs]
version-file = "python/fxhoudinimcp/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["python/fxhoudinimcp"]
# Never ship bytecode caches from the working tree.
exclude = ["**/__pycache__/**", "**/*.pyc"]

# Ship the Houdini-side plugin inside the package. Without this the two halves
# are distributed separately -- server from PyPI, plugin from a git clone -- so a
# `pip install --upgrade` silently leaves the plugin behind. `fxhoudinimcp
# houdini-package` resolves this path and writes the Houdini package file, so
# nobody has to type a site-packages path by hand.
[tool.hatch.build.targets.wheel.force-include]
"houdini" = "fxhoudinimcp/houdini"

[tool.ruff]
target-version = "py310"
line-length = 100
src = ["python"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["fxhoudinimcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "integration: requires a live Houdini instance",
]
